# 活动文档历史快照

本目录保存从活动入口迁出的完整旧文。文件只用于追溯当时状态、命令、版本身份和推理
过程；不得从历史快照直接派生新配置、测试包或“当前下一步”。

当前状态只看：

- `.agents/agent.md`：稳定项目边界与文档路由；
- `.agents/plan.md`：活动任务、blocker 和下一步；
- `.agents/rules/`：当前有效规则；
- `.agents/task_records/`：已完成工作的压缩证据。

