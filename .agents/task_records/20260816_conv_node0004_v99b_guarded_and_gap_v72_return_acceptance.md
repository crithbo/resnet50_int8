# Serialized Conv v99b guarded successor and GAP v72 return acceptance

## Scope

Mainline consumed the family-owned Serialized Conv v98 runtime-failure analysis and the exact GAP v72 formal return, then serialized the managed-storage lifecycle. No server upload, lease, connection, or run was performed.

## Serialized Conv

The user reported that `r5_n4_hw_v98b_lcdup_tuple10` wrote more than 50 GB beneath `/home/panqs/ndp/NDP_copy01/install/codex_runs`, exhausted disk space, and produced no return ZIP. Exact package inspection establishes a high-confidence reachable package defect: every owner-clock edge emitted a full JSONL observer row; `sim.log` and the observer chunk were copied/rewritten during finalization; the 100 MB threshold was warning-only and evaluated too late. Exact remote member sizes remain unknown because no formal return or remote inventory exists.

Fresh `r5_n4_hw_v99b_lcdup_guarded` preserves the LC9-to-LC3 duplicated configuration, workload, numeric artifacts, golden data, functional RTL, tuple10 target, observer-only mode, and retired-ACK exclusion. It removes per-edge clock JSONL while retaining exact periodic heartbeats, applies compile/simulation/finalization growth and free-space guards, preserves all completed rows on a guard stop, fully reaps the process tree, and cleans only the exact attempt/bootstrap roots after a durable return.

Managed pending ZIP:

- path: `artifacts/operator_config_validation/r5-server-test-packages/pending/r5_n4_hw_v99b_lcdup_guarded.zip`
- bytes: 5922296
- SHA-256: `045efa0eea14487c93c9cb88afc959303cbec78683eff867cc31ff64d3c55aa2`

The package binds a maximum transient `install/codex_runs` growth of 10.8 GB above the initial attempt footprint and zero persistent exact-attempt bytes after a durable return. All 15 exact local gates pass. Production behavior, tuple10, natural terminal, Formal-D, E3, E4, and E5 remain unproven.

Consumed v98 was moved to `tested`, bound to `outputs/conv_node0004_v98b_runtime_failure_analysis/runtime_failure_rootcause_report.json`.

## GAP

The exact v72 return proves that the temporary configuration bypass moves A through MSE1/Buffer2 and reaches GA inport1 on all 16 selected slices, so it dynamically bypasses the old MSE0/Buffer0 stale-FIFO path. It does not repair that RTL defect and does not complete the operator: enabled C/GA inport2 remains unavailable, so GA output, MSE4 write, finish, natural terminal, and Formal-D are absent. The result is `CONFIG_BYPASS_FAILED_SAME_OR_NEW_CAUSAL_ROOT`, not a production-equivalent workaround. Current terminal state is `WAIT_RTL_REPAIR_OR_SEPARATELY_AUTHORIZED_C_PATH_ACTION`.

Consumed v72 was moved to `tested`, bound to `outputs/gap_node0071_v72_return_r1786853520713128959_3181659/mainline_return_receipt.json`.

## Storage and claim boundary

Corrected global audit passes at pending/tested/superseded `2/54/24`. Exact pending packages are Serialized Conv v99b and QAdd v68. GAP and native Conv intentionally have no pending package. No QAdd artifact changed.

No functional RTL/config/numeric/workload/golden mutation or server action occurred. Managed-storage writes stopped after the passing post-audit.
