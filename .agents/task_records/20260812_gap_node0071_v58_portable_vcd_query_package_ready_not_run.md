# GAP node0071 v58 portable VCD/query successor

Status: `PACKAGE_READY_NOT_RUN`

Role: `family.gap`  
Owner epoch: `2`  
Registry epoch: `6`

## Previous-version progress

v56 dynamically proved the slice-local-base workaround through `sum_s1` on all 16 selected slices. v57 preserves that workaround and targets the remaining `sum_s2` input-supply/read path before first writeback.

## Current-version purpose

The fresh v58 identity preserves the v57 configuration, numeric payloads, workload, golden data, functional RTL, source-bound target, and GAP diagnostic purpose. It adds locally actionable evidence for the same causal target:

- authoritative full-hierarchy, depth-0, unbounded raw VPD remains enabled;
- a distinct same-attempt `DUMP_PORTABLE_VCD=1` path returns direct unbounded VCD;
- an identity-bound registered query receipt records the exact 48 source-bound candidates, widths, contiguous sequence, every ordered `0/1/X/Z` transition, and end states;
- any portable/query failure preserves raw waveform and compile/sim/signal/return core while reporting `DIAGNOSTIC_EVIDENCE_INCOMPLETE`.

## Closure

The exact final ZIP passed deterministic double construction, source-bound, mandatory VPD, portable query, post-sim, runner definition-before-use, six-exit safe harness, runtime-layout, clean-extract, candidate discrimination, and new-epoch first-fresh audits. The safe harness exercised 48 candidates and 192 ordered events and proved fixed-result publication for natural, preflight-failure, compile-failure, HUP, INT, and TERM exits.

Storage rotation used `tools/manage_server_test_package_storage.py`: unrun v57 moved to `superseded`, v58 is the sole GAP pending ZIP, and the corrected global storage audit is clean.

Machine receipt: `outputs/gap_node0071_v58_sum_s2_portable_vcd_query/completion_receipt.json`

No upload, server run, lease, production compile, DUT simulation, RTL change, config change, numeric change, plan/rule change, or owner-registry change occurred.
