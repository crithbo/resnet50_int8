# Family dispatch/mode binding canonical activation

Date: 2026-08-18

The `family-dispatch-mode-binding-v1` shared workflow is activated for later fresh packages only. Current serialized v103 and current/in-progress QAdd packages are explicitly not held, rebuilt, modified or rotated by this activation.

The project-local `resnet50-server-package-flow` Skill, schema, dispatch contract, validator, tests and audit evidence were mechanically synchronized from the optimizer worktree with their exact source identities. The selector-dispatch source seed was then narrowed only to retain the canonical current observer operational-boundary dispatch receipt; all seven frozen observer assets now match current bytes and SHA-256.

The build-gate registry adds `family_dispatch_mode_binding_final_zip` semantic version 1 with `required_next_fresh_after_activation`. Existing QAdd/TB-VCD runtime-budget increments remain intact: `tb_vcd_bounded_causal_cone_final_zip=8`, `first_fresh_extra_audit=6`, and `runtime_layout=5`.

Future registered-family package dispatch must use the project Skill, resolve the unique ACTIVE persistent owner, send the task by thread message, bind the upstream user/plan/task-record diagnostic-mode authority, and package byte-equal binding plus selector documents. Temporary subagent/child replacement of a registered family role fails closed.

Validation passed: focused 56/56, current related 90/90, Python compile, JSON parse, active-rule audit 14/14 with 164 definitions and no duplicates/errors/warnings, owner-registry unique ACTIVE role check, selector frozen-asset identity check, and `git diff --check`.

Machine receipt: `outputs/family_dispatch_mode_binding_v1/CANONICAL_ACTIVATION_RECEIPT.json`.

No plan, owner registry, current package, managed storage, server, RTL, config, numeric, workload or golden state was modified.
