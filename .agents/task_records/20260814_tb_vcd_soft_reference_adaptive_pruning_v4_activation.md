# TB-VCD soft-reference and adaptive-pruning v4 activation

Date: 2026-08-14  
Status: `CANONICAL_TB_VCD_SOFT_REFERENCE_ADAPTIVE_PRUNING_V4_ACTIVATED`

The user superseded the unactivated hard-v4 interpretation. A same-family current round>=3 receipt and reasonable signal-count range now provide a soft reference, not a standalone numeric release gate. Packages outside the range may pass with a warning when relation, explanation and owner acknowledgement are recorded. A source-bound zero-hop driver for each HIGH candidate is a strong coverage target; a missing leaf remains a visible record-only gap while the actual candidate matrix stays complete and pairwise distinguishable.

Refined successors still bind their exact predecessor, signal add/remove/unchanged sets, candidate preserved/closed/new sets, and pinned RTL/catalog semantic identity. A removed signal records reason, HIGH/MEDIUM/LOW confidence and affected candidates. LOW confidence defaults to retain; HIGH/MEDIUM may be removed by engineering judgment without a per-signal formal irrelevance proof over every preserved candidate. Candidate distinction and all VCD stop/size/flush/reap/return protections remain blocking.

Canonical regression passed 90/90. Python compilation, seven JSON parses, owner-registry validation and active-rule audit all pass; the latter reports 14 active/registered rules, 164 unique definitions and zero duplicates/errors/warnings. The default observer path is unchanged. This activation is required-next-fresh only when optional TB-VCD mode is selected; it does not hold, rebuild or revalidate current v94b/p49/v69/v64 and authorizes no server action.

Machine receipt: `outputs/tb_vcd_soft_reference_adaptive_pruning_v4/canonical_activation_receipt.json`.
