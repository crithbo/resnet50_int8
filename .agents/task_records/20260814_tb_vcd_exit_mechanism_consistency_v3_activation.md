# TB-VCD exit mechanism consistency v3 activation

Date: 2026-08-14  
Status: `CURRENT_DISK_TB_VCD_EXIT_MECHANISM_CONSISTENCY_V3_ACTIVATED`

Three formal returns exposed two exit classes: native p48 and GAP v63 used stale/non-authoritative time witnesses and false-froze while archived VCD timestamps advanced; serialized v93d let its outer runner stop at suspected-only plateau while the shared evaluator correctly rejected the decision. None was a valid natural completion or a qualified true-freeze stop.

The existing optional-VCD public rule is narrowed without adding a synonymous ID. For every later next-fresh optional-VCD package, the shared runtime evaluator receipt is the sole outer-runner decision authority. Exact packaged replay proves advancing timestamp and suspected-only plateau continue, while dump-off-plus-grace plateau and true three-interval freeze stop. Finalization also binds the quiescent archived VCD full-file SHA/bytes/last timestamp to final runtime; incomplete, unflushed, unclosed or unreaped evidence cannot pass.

Canonical focused and related regression passed 77/77, py_compile passed, scoped diff-check passed, and active-rule audit passed with 14 active/registered rules, 164 unique definitions, zero duplicates/errors/warnings. Observer-only remains the default and unchanged. This activation is required-next-fresh only and authorizes no server action.

Machine receipt: `outputs/tb_vcd_cross_family_exit_audit_v1/canonical_activation_receipt.json`.
