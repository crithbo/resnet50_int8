# v100/QAdd v70 formal returns, guard-procfs activation, and v102 publication

The exact serialized v100 return proved production VCS compile/elaboration/link and simv generation, but simulation never started because the guard counted its own transient `ps` enumerator as an owned survivor. This is an implementation escape, not a functional RTL/config/mapper failure. Canonical observer guard semantic v5 now uses childless procfs snapshots with `(PID,start_time_ticks)` identity, preserves real descendants across PID reuse, forbids same-basename return replacement, and admits cleanup only after valid fully-reaped finalization plus durable-return receipts. Canonical guard/boundary regression is 201/201 PASS.

Transitional v101 was rejected before publication because its embedded `ps` helper was not byte-equal to the canonical procfs helper. Fresh serialized v102 preserves the LC9→LC3 mapping, workload/numeric/golden/functional RTL and the 52-signal tuple10/downstream/natural/Formal-D target. Its 22/22 exact gates, 29/29 canonical guard/boundary tests and 5/5 procfs/failure-handoff negative controls pass. Managed storage atomically moved consumed v100 to tested and published v102 as serialized sole pending.

The exact QAdd v70 return proved compile and advancing preload through 19 qualified transfers, but the fixed 3600-second wall ceiling fired before target entry. The target repair remains dynamically unadjudicated. The shared audit recommends a source-bound measured preload budget of 8022 seconds and a bounded 8400-second successor, while retaining independent 8GB VCD, 10GB return, disk, growth, write and quota protections. Because this materially extends server runtime, the formal rule/activation and QAdd successor remain held for explicit user authorization. v70 remains managed pending but is consumed and not runnable again.

Storage post-audit is clean at pending/tested/superseded `2/58/24`; exact pending is serialized v102 plus consumed-held QAdd v70. No server, upload, lease or run action occurred.
