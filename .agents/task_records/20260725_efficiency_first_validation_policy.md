# 效率优先验证策略调整

日期：2026-07-25

- 本地开发改为增量检查，不在每个中间步骤重复全量重建或全项目回归；
- node0001 原子动态验证默认合并为一个 single-occurrence two-stage 组合测试；
- guard-only、round-only 和额外 alias 诊断只按最早分歧启用；
- 服务器候选首次形成或输入/生成器身份改变时，执行一次完整交接前自检；
- 冻结身份未变时复用已有收据；
- 本次只修改执行计划，不改变算子语义、JSON、码流或 RTL，因此未重建语义总账。

最终 `.agents/plan.md` 为 180 行，SHA-256：
`40abbb9467c0125ea1a4023c123a401d300d6fa3f4e7542c8f9bb0fb37976e59`。
plan 约束测试 7/7 通过，`NDP_copy01/rtl/` 无改动。
