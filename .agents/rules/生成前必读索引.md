# 生成前必读索引

最后更新：2026-08-14（observer 默认 + 可选 TB VCD 全因果锥双模式）

本文件只负责“读什么、谁拥有语义、何时停止”。字段公式、服务器实现、算子细节和版本
结果分别由其唯一 owner 文件、plan 与 task record 保存，不在这里复写。

## 1. 固定入口与读取收据

每个会话首次工作必须完整读取 `.agents/agent.md`、本文件和 current `.agents/plan.md`。
随后只按本轮 changed surface 选择下表规则。活动规则 exact-set 与职责以
`contracts/active_rule_registry_v1.json` 为准；`.agents/history/rules/` 默认禁止作为输入。

同一轮同一文件只读一次，并记录 path、SHA-256、reason 和 read time。规则、活动源码、
机器合同或原始回传冲突时立即停止并报告，不选择“最方便”的值。

## 2. 按动作路由

| 动作 | 必读 | 条件叠加 |
|---|---|---|
| 新建/修改 complete JSON、mapping、bitstream、execplan 或 SCA | `算子配置规则.md`、`NDP硬件字段语义.md`、目标族/原语规则 | 实际被调用的原生 README、handler、mapper、encoder、planner、RTL consumer |
| 只消费已冻结 byte-equal 资产 | `算子配置规则.md` 的 reuse/provenance 门与资产 manifest | 只有 changed leaf 才读取对应硬件/族规则 |
| 生成或修改服务器测试包、runner、observer/VCD、return | `服务器测试包生成规则.md`、服务器入口 README、目标族动态门 | workload/config 改变时加 `算子配置规则.md`；凡可能进入 DUT simulation，先读取 `contracts/server_diagnostic_mode_selector_dispatch_v1.json` 并显式选择恰好一个bulk模式。默认observer读取`contracts/server_observer_only_wide_causal_dispatch_v1.json`且原路径不变，并合取`contracts/server_observer_operational_attempt_boundary_dispatch_v1.json`：100MB仍只告警，next-fresh必须用族内来源的六项峰值+reserve做准入、分别监控compile/simulation/finalization，只能一次停止整个attempt并在durable partial return后精确清理，禁止截断/采样/滚动覆盖/按大小删证据。可选VCD读取`contracts/server_tb_vcd_bounded_causal_cone_dispatch_v1.json`，仅允许package-local TB标准dump tasks与source-bound全因果锥，并要求exact catalog `$dumpvars`、append VCD timestamp freeze、unsigned>=64位/16384-cycle heartbeat、multiline timescale和partial/flush/reap fail-closed；首轮绑定同族当前`round>=3`参考和合理信号量范围，但数量为软参照，偏离范围可带说明/确认告警通过；HIGH候选zero-hop driver为强目标，缺失可record-only但matrix必须仍可区分。后续包递归绑定predecessor及精确增删差异；删信号记录理由、置信度和受影响候选，LOW置信度默认保留，HIGH/MEDIUM可工程删减而无需逐项形式化证明。outer runner只能消费共享evaluator决定，archive SHA/bytes/last-timestamp必须绑定final runtime，并执行advance/suspected/full-plateau/true-freeze四态回放。两种模式actual argv均固定`DUMP_VCD=0,DUMP_FSDB=0,TB_DUMP_FSDB=0`，VPD/FSDB/UCLI direct-VCD/vendor query/full-top dump均禁止；必须读取`contracts/server_runtime_preflight_native_flow_dispatch_v1.json`，production前禁止文件/tool/provider existence、Make dry-run或短探针，编译完整性由真实compile自然裁决；final release另读取`contracts/server_package_release_admission_dispatch_v1.json`，在manifest晋升后对staging与clean exact-ZIP执行package-specific preflight和pending-status负控，并以schema-enabled repository Python runtime逐一编译两侧exact-set全部`.py`、实际执行JSON schema validation，依赖缺失/skip/schema失败不得PASS；全部package-local HDL执行staging+exact-ZIP lexical/full frontend/scope/state门；next-fresh只运行所选模式的final-ZIP gate并执行first-fresh独立复核 |
| 分析正式 return 并设计 successor | `服务器测试包生成规则.md` 的 return/result/successor、所选模式本机可读门、流式分析/三槽保留合同、`contracts/server_runtime_preflight_native_flow_dispatch_v1.json` 的失败后 native differential 与目标族动态门 | observer直接消费catalog/chunks/end-state/decision；VCD使用`tools/server_tb_vcd_retention_analysis.py`流式读取ZIP/VCD并同步写`analysis_state.json`、append-only checkpoints和`report.md`，不得把大文件整体放入会话上下文；先闭合LAST_PROVEN_GOOD/FIRST_DIVERGENCE；production失败比较actual cwd/argv/env/log与原生ndp-sim/活动Make/TB/filelist，未知服务器命令不得猜测或变成preflight |
| 会话替换、owner 切换或整批换代 | `会话转接与所有权规则.md`、current owner registry | 目标 role 专项规则、latest record、current pointer 与 in-flight receipt；整批换代 mainline first |
| 整网测试收敛/共享 validator 审计 | `整网测试收敛优化专项规则.md`、相关公共规则 | 按失败机制选读 machine report、task record、generator 与 validator |
| 功能 RTL repair | 目标数值/硬件/服务器规则 | 必须有本轮用户明确 RTL 授权；历史 repair 文件不构成授权 |

服务器诊断包还必须沿唯一公共定义路由到
`CDA-SERVER-SOURCE-BOUND-GENERATED-OBSERVER-001`、
`CDA-SERVER-ALWAYS-ON-TRIGGERED-CAUSAL-OBSERVABILITY-001`、
`CDA-SERVER-DIAGNOSTIC-QUALIFIED-BUDGET-NOT-CONSUMED-BY-STATE-001`、
`CDA-SERVER-DIAGNOSTIC-LOGGER-PARSER-EXACT-FORMAT-TRACE-001` 与
`CDA-SERVER-DIAGNOSTIC-MULTICLASS-EDGE-NO-LOSS-001`；本索引只引用这些 rule ID，
不复制其语义定义。

## 3. 按算子族路由

| 目标族 | 专项/原语增量 |
|---|---|
| QLinearConv accumulate | `INT8_SA点积专项规则.md` |
| QLinearConv output Requant | `RequantizeUint8算子配置规则.md` + `精确UINT8量化尾专项规则.md` |
| QLinearMatMul | `INT8_SA点积专项规则.md`；输出量化再叠加 Requant/exact-tail |
| QLinearAdd | `QLinearAdd算子配置规则.md` + `精确UINT8量化尾专项规则.md`；实际跨 stage scratch 再加 `最小双Stage生命周期规则.md` |
| QuantizeLinear | `精确UINT8量化尾专项规则.md` |
| RequantizeUint8 | `RequantizeUint8算子配置规则.md` + `精确UINT8量化尾专项规则.md` |
| DequantizeLinear | `DequantizeLinear算子配置规则.md`；跨 stage scratch 再加 `最小双Stage生命周期规则.md` |
| GlobalAveragePool | `GAP_int32_mac_bypass_rules.md` + `精确UINT8量化尾专项规则.md`；跨 stage scratch 再加生命周期规则 |
| metadata-only Flatten/View | `Flatten_View算子配置规则.md` |
| MaxPoolUint8 | 无族专项；只读公共配置与实际硬件 consumer |

从 `release-cross-member-temporal-consistency-v1` 激活后的 next-fresh 服务器包，顶层 final release
driver 还必须一次运行 `tools/validate_server_release_consistency.py`，聚合检查 release-critical 嵌套
状态、selected/absolute budget 跨成员身份与角色分离、return producer/phase 闭合、guard→publish→
durable→cleanup 时序，以及 source-span-bound progress/held-level replay。该门只阻断
`state_safety/return`，不替代族级配置、数值、RTL 或动态裁决。

本地候选若从未进入 managed storage、从未运行、从未作为权威 release 交付且未绑定 formal return，
可按 `服务器测试包生成规则.md` 1.3 节保留原 identity 定点修补：必须绑定 prepatch SHA、逐文件 exact
delta 和冻结功能面，旧 final-ZIP/ready 收据全部失效；`receipt_reuse=false` 或依赖输入变化的门重跑，
只有 `receipt_reuse=true` 且 exact inputs/validator/schema/authority 全部不变的门可复用。任何已托管、
已发布、已运行或已有正式 return 的包仍不可变并必须使用 fresh successor。

未命中适用范围的专项规则不读，也不得借其历史授权扩大权限。族规则只拥有本族差异，
公共规则只拥有跨族合同；同一 `CDA-*` 定义不得复制。

## 4. 原生文件选择

按实际调用路径选读：

- tensor/golden/relayout：目标生成脚本、正式输入和其直接 README；
- graph/地址/execplan/SCA：目标 op_json、handler、planner、output writer 和 pipeline；
- mapping/bitstream：目标 mapper、encoder 和最终 consumer；
- 服务器包：活动 Makefile/TB/filelist、`NDP_copy01/README_HARDWARE_SIM_ENTRY.md`；
- 隔离适配：绑定 manifest、被替换源码及 changed-surface consumer。

未被命令调用、未影响最终字段、也不参与验收的文档不列入必读，但需在 receipt 中说明
为何不适用。活动 `ndp-sim`、RTL、ISA 默认只读。

## 5. 公共停止门

出现任一项必须停止当前生成/发布，并报告最后可信边界：

1. target exact stage、typed request、qparam、layout、workload 或输入 identity 不完整；
2. final leaf 无唯一 owner，source absent 被隐式补零，handler 对 changed axis 无能力，或
   composite boundary 仍 unresolved；
3. 计划从历史失败包、服务器残留、停用规则或来源不明产物补文件；
4. final JSON 无法反解到实际 RTL/mapper/encoder consumer，或地址、transaction、bank/row、
   lifetime、terminal、formal D coverage 未闭合；
5. 把本地 E0/E1/E2、schema pass、自然完成或内部 write 当作 E4/E5；
6. 准备改 RTL/ISA/硬件、执行服务器或改变外部状态，但没有本轮用户授权；
7. 生成服务器包却未完整读取 current `服务器测试包生成规则.md`，或 shared gate 发现会
   影响 server start、actual input、state safety、return 的问题；所有廉价错误必须一次
   聚合，record-only 项不得阻断；package-local HDL staging/final ZIP 的共享保留字声明名
   报告缺失、非空或身份漂移属于 `server_start` 阻断，不能等服务器 compile 才发现；
   final staging与clean exact-ZIP的package-local `.py` path/bytes/SHA exact-set不一致、任一成员未通过
   非污染式compile，或blocking schema gate的`jsonschema`依赖缺失/不兼容、skip、未执行/schema failure
   同样必须停止，不能把环境性skip记作PASS；
    production launch前若runner检查服务器文件/目录/tool/library/module provider存在性、执行Make
    dry-run或短探针必须停止；只能由真实production cwd/argv/log/exit裁决编译完整性，失败后再做只读
    native-flow differential，不得把路径不存在的猜测变成preflight阻断；
8. next-fresh 未显式选择恰好一个bulk模式，或运行了非所选模式的bulk证据。observer模式未满足current
   source-bound role/candidate/logger/parser/4-state event/end-state/heartbeat/partial-exit/first-use门；VCD模式
   未满足41-role source-bound catalog、四层FIRST_DIVERGENCE边界、完整且两两可区分的candidate×boundary
   矩阵、首轮同族当前第三轮软参考/偏离说明、refined successor精确增删、删信号理由/置信度/受影响候选
   （LOW默认保留）、TB标准dump tasks、严格plateau交集、共享evaluator唯一决定、archive-tail绑定、四态回放、
   独立安全终止、VCD close/flush和first-fresh门；任一模式
   actual Make dump非零、出现VPD/FSDB/UCLI direct-VCD/vendor query/full-top dump、derived expected替代
   actual net、100,000,000-byte软偏好被实现为hard cap，或存在截断/采样/按大小删除时必须fail closed；
   simulation_started=true却缺所选模式required evidence时保留已有core/partial证据并标记
   `DIAGNOSTIC_EVIDENCE_INCOMPLETE`；
9. 正式 return 尚未闭合目标，又未进入 `CLOSED`、`WAIT_RTL_FIX`、
   `HARDWARE_CAPABILITY_BLOCKED` 或 `WAIT_USER_DECISION`，却准备无 successor 结束；
10. 会话换代无合法 capsule/acceptance/单 role activation，出现重复 ACTIVE owner，或正式
    return 仍硬编码旧 mainline thread 而未动态解析 registry；
11. package/return/规则任务结束前未向 current mainline 回传结构化结果、claim boundary 与
    有证据的 `RULE_DELTA_PROPOSAL` 或 `RULE_CONFIRMATION`；
12. 活动规则集合、职责、SHA 或唯一 `CDA-*` owner 不通过
    `tools/audit_active_rule_registry.py`。
13. 每个family只允许保留`MAX_PROGRESS + LATEST_1 + LATEST_2`三组重型原始结果；source package、return、
    sidecar与raw evidence按组保护。分析未完成、family/mainline未双消费、确定性core证据未闭合或保护集
    审计未通过时不得删除；第四组到达后须在当前分析事务结束前安全恢复到三组。
14. next-fresh runner 未用 Linux child-subreaper + fresh session/process group + 内部 timeout 监督完整
    simulator tree，未把仿真时间心跳与 host liveness 分离，或在 root/adopted descendant 未全部终止
    回收、当前模式evidence未close/flush且exact-set未稳定时开始归档；失败必须保留已有PARTIAL evidence/
    core return并标记
    `DIAGNOSTIC_EVIDENCE_INCOMPLETE`，不得以 return 已发布或 simv root exit 代替。
15. production compile成功、simulation和目标因果区间实际执行、return可消费，但仍无法一轮唯一定位
    根因时，未在successor前完成`RULE_GAP_AUDIT`，或审计未覆盖catalog/matrix/source identity/
    stop/global witness/return/parser/正负控，未把裁决绑定到下一包first-fresh/final-ZIP；
16. 同一目标连续两次fresh构包/final gate失败，或连续两次服务器尝试被package-local缺陷挡在目标
    之前，却在第三次尝试前未完成`PACKAGE_BUILD_FAILURE_RULE_AUDIT`；仅改package identity重试也
    视为同一连续失败链。

审计是强制的，但规则修改不是强制的。若审计证明current规则/门已充分覆盖且只是孤立偶发实现或人工
操作失误，可输出`RULE_CONFIRMATION_NO_CHANGE`，只修package并重跑原门；不得为满足审计形式新增同义
规则或无因果价值的validator/负控。

详细阻断条件只在其 owner 规则中定义；本节不复制实现清单。旧/current package 是否追溯
适用、shadow/blocking disposition 和 first-fresh 附加查验，全部按 current server rule。

## 6. 证据等级与回归分类

这是 E0–E5 的唯一公共定义：

- E0：静态可解析/可编码；
- E1：来源、结构、mapping、bitstream、execplan、地址和确定性闭合；
- E2：独立软件公式、layout、golden 和最终物化回环闭合；
- E3：绑定身份的服务器运行自然完成；
- E4：正式 readback 与独立 golden 一致；
- E5：全新身份重复运行并覆盖专项边界，可推广。

`REUSE_ACCEPTED_FOR_INTEGRATION` 只是免复测装配状态，不属于 E0–E5。

回归分类唯一口径：有同门 known-good 才称 `REGRESSION`；只有本地/静态基线称
`NO_DYNAMIC_BASELINE`；首次服务器失败称 `FIRST_DYNAMIC_FAILURE`。

## 7. 最小读取收据

```json
{
  "read_receipt": [
    {
      "path": ".agents/rules/生成前必读索引.md",
      "sha256": "<sha256>",
      "reason": "routing"
    }
  ],
  "rule_ids": [],
  "changed_surfaces": [],
  "known_counterexamples": [],
  "open_dynamic_gates": [],
  "omitted_files": [
    {"path": "<not-read>", "reason": "not consumed and no affected field"}
  ]
}
```

receipt 只证明读取范围和身份，不证明语义正确。最终放行仍由目标 validator、独立 golden
和对应动态门决定。
