# ResNet50 INT8 agent entry

This file is only the automatic repository entry for Codex-compatible agents.
Before doing any project work, read `.agents/agent.md` completely and follow
its role-specific routing. Current state belongs to `.agents/plan.md`; this
file intentionally duplicates no project rule or status.
